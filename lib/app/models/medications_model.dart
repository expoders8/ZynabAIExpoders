class MedicationssModel {
  String image, title, decription, dates, badge;
  bool addmedications;
  MedicationssModel({
    required this.image,
    required this.title,
    required this.decription,
    required this.dates,
    required this.badge,
    required this.addmedications,
  });
}
