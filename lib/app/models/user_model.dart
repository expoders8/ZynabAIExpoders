class UserModel {
  String image, name, dateTime, money, number;
  UserModel({
    required this.image,
    required this.name,
    required this.dateTime,
    required this.money,
    required this.number,
  });
}
