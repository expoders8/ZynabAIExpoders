class Transaction {
  String image, name, dateTime, money, number;
  Transaction({
    required this.image,
    required this.name,
    required this.dateTime,
    required this.money,
    required this.number,
  });
}
